#include <iostream>
#include <xtree>
using namespace std;

int main() {

	int t;
	cin >> t;
	for (int i = 0; i < t; i++) {
		int n, k;
		cin >> n >> k;
		int*time = new int[n];
		for (int j = 0; j < n; j++) {
			cin >> time[j];
		}
		_Tree<int> T(1);
		T.
	}
}